# MCPY

## MCP + Python = MCPY

- A lightweight framework for building MCP servers in Python
- Supports tool registration, resource handling, and prompt templates
- Easy to install via pip with zero configuration required
